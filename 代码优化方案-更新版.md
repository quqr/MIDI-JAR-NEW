# 代码优化方案（更新版）：基于严格行数标准

> 扫描日期：2026-07-20
> 扫描范围：`F:\Codes\MIDI-JAR-NEW\src` 全部源代码文件
> 行数标准：理想 200-400 行 | 警戒 500 行 | 重构红线 800-1000 行

## 一、执行摘要

根据用户提供的新行数标准重新扫描，发现 **9 个** 需关注的源代码文件，其中 4 个超过重构红线/警戒线（500+ 行），5 个处于警戒区（400-500 行）。本方案更新目标行数至 200-400 行，新增 ESLint `max-lines` 规则配置建议，并基于"症状信号"标准诊断每个文件。

**关键发现**：
- `WaterfallPiano.vue` 含 37 个 if/switch，是严重"裤子"文件
- `PianoKeyboard.vue` 含 54 个函数，职责过多
- `FluidSolver.ts` 因算法密集特性豁免重构

---

## 二、行数标准（用户定义）

| 区间 | 状态 | 处理方式 |
|------|------|---------|
| 200-400 行 | 理想（健康） | 无需操作 |
| 400-500 行 | 警戒区 | 评估症状，考虑拆分 |
| 500-800 行 | 需重构 | 必须拆分 |
| 800-1000+ 行 | 重构红线 | 强烈建议重构（除非是纯数据/自动生成） |

---

## 三、汇总表格（9个文件，按行数降序）

| 文件路径 | 行数 | imports | 函数数 | if/switch | 状态 | 症状诊断 |
|---------|------|---------|--------|-----------|------|---------|
| `src/views/WaterfallPiano/engine/NoteBlockSystem.ts` | 911 | - | - | - | **重构红线** | 5种职责，单方法5000字节，严重违反SRP |
| `src/views/WaterfallPiano/components/WaterfallSettingsContent.vue` | 785 | - | - | - | **需重构** | 7组选项硬编码，UI过载，选项数据与视图强耦合 |
| `src/views/WaterfallPiano/engine/WaterfallEngine.ts` | 717 | - | - | - | **需重构** | startLoop 6466字节，工具函数未外移 |
| `src/views/Settings/AdvancedDebug/AdvancedDebug.vue` | 690 | - | - | - | **需重构** | 6个重复配置更新函数，大量重复代码 |
| `src/views/ChordDictionary/Detail/ChordDetail.vue` | 507 | 16 | 35 | 7 | **警戒区** | 多重导入（16个），函数偏多（35个） |
| `src/components/PianoKeyboard/PianoKeyboard.vue` | 505 | 15 | 54 | 10 | **警戒区** | 54个函数，职责过多，10个if/switch |
| `src/views/Layout/AppNavbar.vue` | 456 | 10 | 19 | 4 | **警戒区** | 症状轻微，可观察 |
| `src/engine/fluid/FluidSolver.ts` | 490 | 4 | 20 | 10 | **警戒区** | 算法密集，**豁免** |
| `src/views/WaterfallPiano/WaterfallPiano.vue` | 436 | 18 | 55 | 37 | **警戒区** | **"裤子"文件**（37个if），18个import，55个函数 |

---

## 四、重构症状诊断

根据用户提供的"症状信号"标准：

### 1. "裤子"文件（大量 if/switch）
- `WaterfallPiano.vue` — **37 个 if/switch**，严重症状，需用策略模式重构
- `PianoKeyboard.vue` — 10 个 if/switch，中等症状

### 2. 多重导入（依赖过多，内聚性差）
- `WaterfallPiano.vue` — 18 个 import
- `ChordDetail.vue` — 16 个 import
- `PianoKeyboard.vue` — 15 个 import

### 3. 函数过多（职责混乱）
- `WaterfallPiano.vue` — 55 个函数
- `PianoKeyboard.vue` — 54 个函数

### 4. 算法密集型（可豁免）
- `FluidSolver.ts` — 仅 4 个 import，20 个函数，490 行流体模拟算法实现，符合"纯数据/算法文件"豁免条件

---

## 五、优化方案

### A. ESLint 规则配置（新增）

**文件**: `.eslintrc.js`（新建）

配置 `max-lines` 规则，防止未来再次出现超大文件：

```javascript
module.exports = {
  rules: {
    // 警告：超过 500 行
    "max-lines": ["warn", { max: 500, skipBlankLines: true, skipComments: true }],
  },
  overrides: [
    {
      // 纯数据/配置文件豁免
      files: ["**/constants/**", "**/data/**", "**/*.config.ts"],
      rules: { "max-lines": "off" }
    }
  ]
};
```

---

### B. 500+ 行文件重构方案（4个文件，目标 200-400 行）

#### B1. NoteBlockSystem.ts (911行 → ~300行)

**问题核心**：一个类承担 5 种职责（对象池、双模式逻辑、渲染、引用计数、状态重建）

**拆分策略**：
```
src/views/WaterfallPiano/engine/
├── NoteBlockSystem.ts          (协调器，~300行)
├── NoteBlockPool.ts            (对象池，~100行)
├── SynthesiaModeController.ts  (synthesia模式，~250行)
├── RealtimeModeController.ts   (realtime模式，~150行)
├── NoteBlockRenderer.ts        (渲染，~100行)
└── NoteBlockStateSync.ts       (状态重建，~100行)
```

**具体操作**：
1. `NoteBlockPool.ts` — 移出 `acquire()`, `release()`, `pool`, `getPoolSize()`
2. `SynthesiaModeController.ts` — 移出 `scheduleSynthesiaNotes`, `markSynthesiaNotePressed`, `releaseSynthesiaNote`, `updateSynthesiaBlocks`（拆分为 `advanceCursor`/`createBlocks`/`triggerNotes`/`endNotes`/`handleSeek`）, `rebuildTriggeredSet`
3. `RealtimeModeController.ts` — 移出 `playRealtimeNote`, `releaseRealtimeNote`, `playRealtimeNoteFromMidi`, `releaseRealtimeNoteFromMidi`, `addActiveMidi`, `removeActiveMidi`, `triggeredSet`
4. `NoteBlockRenderer.ts` — 移出 `render()`, `roundRect()`
5. `NoteBlockStateSync.ts` — 移出 `syncStateToTime`
6. `NoteBlockSystem.ts` — 保留为门面，委托给子控制器

---

#### B2. WaterfallSettingsContent.vue (785行 → ~200行)

**问题核心**：选项数据与视图耦合，模板臃肿

**拆分策略**：
```
src/views/WaterfallPiano/components/
├── WaterfallSettingsContent.vue       (主容器，~200行)
├── sections/
│   ├── ColorSchemeSection.vue         (~80行)
│   ├── FluidStyleSection.vue          (~80行)
│   ├── KeyboardSection.vue            (~100行)
│   └── AuraSection.vue                (~100行)
src/views/WaterfallPiano/config/
└── options.ts                         (选项列表，~80行)
```

**具体操作**：
1. `config/options.ts` — 移出 7 组选项列表，导出为接收 `t` 翻译函数的工厂函数
2. `sections/*.vue` — 按功能分区拆分模板，通过 props/emit 通信
3. 主容器简化为 `<SettingsCollapse>` 包裹子组件

---

#### B3. WaterfallEngine.ts (717行 → ~400行)

**问题核心**：`startLoop` 单方法 6466 字节过大

**拆分策略**：
```
src/views/WaterfallPiano/engine/
├── WaterfallEngine.ts          (协调器，~400行)
├── RenderLoop.ts               (主循环，~150行)
└── colorUtils.ts               (颜色工具，~50行)
```

**具体操作**：
1. `RenderLoop.ts` — 移出 `startLoop`，拆分为 `advancePlayback`/`renderFrame`/`updateFluid`/`applyPersistentSplats`
2. `colorUtils.ts` — 移出 `hexToRgb`, `hslToRgb`（纯函数）
3. `WaterfallEngine.ts` — 保留协调职责，持有 RenderLoop 实例

---

#### B4. AdvancedDebug.vue (690行 → ~300行)

**问题核心**：6 种配置更新函数高度重复

**拆分策略**：
```
src/views/Settings/AdvancedDebug/
├── AdvancedDebug.vue                  (主容器，~300行)
├── composables/
│   ├── useConfigUpdater.ts            (通用更新器，~80行)
│   └── useAdvancedDebugOptions.ts     (选项，~60行)
└── sections/
    ├── NotationDisplaySection.vue     (~80行)
    ├── NotationLayoutSection.vue      (~80行)
    ├── NotationStyleSection.vue       (~80行)
    ├── WaterfallBackgroundSection.vue (~100行)
    ├── WaterfallKeyboardSection.vue   (~80行)
    └── WaterfallSoundSection.vue      (~80行)
```

**具体操作**：
1. `useConfigUpdater.ts` — 抽象通用更新器（消除6个重复函数，约-200行）：

```typescript
function createConfigUpdater<T>(
  store: { updateSetting: (path: string, value: T) => void },
  path: string,
  currentRef: Ref<T>,
) {
  return (key: keyof T, value: T[keyof T]) => {
    const current = { ...currentRef.value };
    current[key] = value;
    store.updateSetting(path, current);
  };
}
```

2. `useAdvancedDebugOptions.ts` — 移出 `oscTypeOptions`, `flowDirectionOptions`
3. `sections/*.vue` — 按配置域拆分模板

---

### C. 400-500 行警戒区文件处理（5个文件）

#### C1. WaterfallPiano.vue (436行, 37个if) — **必须重构**

**症状**: 37 个 if/switch（"裤子"文件），18 个 import，55 个函数

**拆分策略**：
```
src/views/WaterfallPiano/
├── WaterfallPiano.vue              (主视图，~200行)
├── composables/
│   ├── useWaterfallEngine.ts       (引擎生命周期，~80行)
│   ├── useWaterfallMidi.ts         (MIDI事件处理，~100行)
│   └── useWaterfallUi.ts           (UI状态管理，~80行)
└── strategies/
    └── modeStrategies.ts           (策略模式替代if/switch，~60行)
```

**具体操作**：
1. 用策略模式替代大量 if/switch — 将模式判断逻辑提取为对象映射
2. `useWaterfallEngine.ts` — 移出引擎初始化、销毁逻辑
3. `useWaterfallMidi.ts` — 移出 MIDI 事件处理逻辑
4. `useWaterfallUi.ts` — 移出 UI 状态管理逻辑

---

#### C2. PianoKeyboard.vue (505行, 54个函数) — **建议重构**

**症状**: 54 个函数，15 个 import，职责过多

**拆分策略**：
```
src/components/PianoKeyboard/
├── PianoKeyboard.vue               (主组件，~250行)
├── composables/
│   ├── useKeyLayout.ts             (键盘布局计算，~80行)
│   ├── useKeyInteraction.ts        (交互处理，~100行)
│   └── useKeyRendering.ts          (渲染逻辑，~80行)
└── KeyComponent.vue                (单键组件，~60行)
```

---

#### C3. ChordDetail.vue (507行, 16 imports) — **建议重构**

**症状**: 16 个 import，35 个函数

**拆分策略**：
```
src/views/ChordDictionary/Detail/
├── ChordDetail.vue                 (主组件，~250行)
├── components/
│   ├── ChordNotesDisplay.vue       (音符显示，~80行)
│   ├── ChordIntervalsTable.vue     (音程表，~80行)
│   └── ChordAlternatives.vue       (替代和弦，~80行)
└── composables/
    └── useChordDetail.ts           (数据和逻辑，~80行)
```

---

#### C4. FluidSolver.ts (490行) — **豁免，不重构**

**理由**:
- 仅 4 个 import，20 个函数
- 是流体模拟算法实现，类似"纯算法文件"
- 拆分会破坏算法的连续性和可读性
- 符合用户标准中的"除非是纯数据配置文件或自动生成的代码"豁免条件

---

#### C5. AppNavbar.vue (456行) — **观察，暂不重构**

**理由**:
- 仅 10 个 import，19 个函数，4 个 if
- 症状轻微，行数接近警戒线下限
- 可观察，若后续增长再重构

---

### D. 类型定义提取（通用建议）

根据用户提到的"TypeScript特殊性"：如果类型定义占比大，移至 `types.ts`。

**操作**: 检查所有重构文件，将 `interface`/`type` 定义移至同目录的 `types.ts`，保持主逻辑文件精简。

---

## 六、优先级排序

| 优先级 | 文件 | 行数 | 核心问题 | 目标行数 |
|-------|------|------|---------|---------|
| P0 | NoteBlockSystem.ts | 911 | 重构红线，5种职责 | ~300 |
| P0 | WaterfallPiano.vue | 436 | 37个if（"裤子"文件） | ~200 |
| P0 | AdvancedDebug.vue | 690 | 6个重复函数 | ~300 |
| P1 | WaterfallSettingsContent.vue | 785 | UI过载 | ~200 |
| P1 | PianoKeyboard.vue | 505 | 54个函数 | ~250 |
| P1 | WaterfallEngine.ts | 717 | startLoop过大 | ~400 |
| P2 | ChordDetail.vue | 507 | 16个import | ~250 |
| P3 | AppNavbar.vue | 456 | 症状轻微 | 观察 |
| - | FluidSolver.ts | 490 | 算法密集，豁免 | 不变 |

---

## 七、架构兼容性说明

所有优化方案均与现有项目架构兼容：

1. **协调器模式保留**：`WaterfallEngine` 和 `NoteBlockSystem` 作为门面继续存在，只委托不实现
2. **Vue 组件结构一致**：拆分后的 `sections/*.vue` 与现有 Settings 组件结构一致
3. **Store 模式不变**：所有配置更新仍通过 `useSettingsStore` 和 `useWaterfallPianoStore`
4. **策略模式引入**：用于替代 `WaterfallPiano.vue` 中的大量 if/switch
5. **Composables 模式扩展**：新增 `useConfigUpdater`、`useWaterfallEngine` 等，与现有 `useNotes`、`useChordDetection` 模式一致

---

## 八、验证步骤

1. **ESLint 验证**: `npx eslint src/ --rule 'max-lines: ["warn", {max: 500, skipBlankLines: true, skipComments: true}]'` 无警告
2. **类型检查**: `npx vue-tsc --noEmit` 通过
3. **测试**: `npm run test` 全部通过
4. **行数验证**: 重新扫描，确认：
   - 无 500+ 行文件（FluidSolver.ts 490行豁免除外）
   - 警戒区文件（400-500行）数量减少
5. **功能验证**:
   - WaterfallPiano：realtime/synthesia 模式正常
   - PianoKeyboard：键盘交互正常
   - ChordDictionary：和弦详情正常
   - AdvancedDebug：所有配置可更新和重置
6. **导入验证**: `grep -r "from.*engine" src/` 确认所有导入路径正确
