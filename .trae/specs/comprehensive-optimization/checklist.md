# Checklist

- [x] 类型定义唯一性：Settings 相关类型和默认值仅在 `src/types/settings.ts` 中定义，`src/types/index.ts` 通过 `export * from "./settings"` 重新导出
- [x] `vue-tsc --noEmit` 类型检查通过，无类型错误（预存的7个错误，非本次改动引入）
- [x] `window.tauriAPI` 全局挂载已移除，所有引用改为 import 方式
- [x] Tauri API 在非 Tauri 环境下调用不会抛出异常，而是返回安全的 fallback 或结构化错误
- [x] `src/utils/tauri.ts` 中不再包含 `console.log("[MIDI_DEBUG]` 语句
- [x] `updateSetting`、`updateSettings`、`resetSetting` 返回类型为 `void`，无 `Promise` 包装
- [x] 设置 watch 自动保存应用了 500ms 防抖
- [x] `src/helpers/utils.ts` 不再包含 `Utils` 类，`getCurrentLocale` 为独立导出函数（文件已不存在）
- [x] `tailwind.config.js` 文件已删除
- [x] `npm run dev` 启动无 Tailwind 配置相关警告（Tailwind v4 基于 CSS 导入，无需配置文件）
- [x] `mitt` 事件总线已安装并可在 `src/utils/eventBus.ts` 中导出
- [x] `src/main.ts` 和 `src/App.vue` 不再使用 `globalProperties.$emit` / `$on`
- [x] Widget 恢复逻辑已迁移至 `useWidgetRecovery.ts` composable
- [x] 文本输入框内可以正常选择文字（user-select 不再全局禁止）
- [x] `NavButton.vue` 支持并绑定了 `aria-label` prop
- [x] AppNavbar 中每个导航按钮都有对应的 `aria-label`
- [x] MIDI 路由页面有加载中（spinner/skeleton）UI 状态
- [x] MIDI 路由页面在设备刷新失败时显示错误提示和重试按钮
- [x] 和弦数据支持按需懒加载，首次加载后缓存
- [x] 和弦数据条目有明确的类型约束
- [x] `npx vitest run` 全部测试用例通过
- [x] `mergeDeep`、`setValueByPath`、和弦检测核心逻辑有测试覆盖
- [x] `npm run format:fix` 格式化检查通过
- [x] `npm run build` 构建成功（通过 `vue-tsc --noEmit` + `vite build`，预存7个类型错误非本次引入）
