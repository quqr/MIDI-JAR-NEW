# CONTEXT.md — 领域术语表

本项目（MIDI-JAR）的领域概念。仅登记术语含义，不含实现细节。

## 核心领域

- **和弦显示（Chord Display）**：实时可视化用户弹奏的和弦（五线谱 + 钢琴 + 和弦名）。
- **和弦词典（Chord Dictionary）**：查询、分组浏览和弦的结构与排列。
- **和弦测验（Chord Quiz）**：系统出题、用户选择正确和弦名的测验。
- **乐谱滚动（Score Scroll）**：MusicXML 乐谱随播放滚动可视化的模块，时间轴由乐谱自身的音符时值与速度标记决定。
- **三维乐谱（3D Score）**：把整首曲子铺入三维空间、随播放推进视角的可视化模块。与乐谱滚动的区别：后者是二维视口内谱面相对固定扫描线移动，前者是静态场景内视角沿时间轴移动。

## 乐谱滚动（Score Scroll）词汇

- **扫描线（Scanline）**：视口内固定位置的参考线，当前播放位置对齐于此，谱面相对其滚动。
- **吸顶位置（Snap Position）**：滚动换行时，新谱面行在视口内的吸附锚点位置。
- **音符命中（Note Hit）**：MIDI 音符事件与乐谱音符在时间（拍位容差内）与音高上的匹配成功。
- **音符命中特效（Hit Effect）**：命中触发的高光框与粒子爆散动画。
- **音乐字体（Music Font）**：谱面音符符号使用的 SMuFL 字体（Bravura / Petaluma / Gonville）。

## 三维乐谱（3D Score）词汇

- **三维乐谱（3D Score）**：把整首曲子的音符一次性摆进三维空间、随播放推进视角的可视化模块。
- **时间轴（Time Axis）**：场景中展开时间维度的方向，单位为拍（beat），经速度段换算为秒。
- **音高轴（Pitch Axis）**：场景中展开 MIDI 音高维度的方向。
- **声部轴（Staff Axis）**：场景中区分谱表的维度；每个谱表在声部轴上占据独立的一层。
- **播放头（Playhead）**：场景中标记当前播放时刻的位置。与「扫描线」的区别：扫描线是二维视口内**固定**、谱面相对其移动的参考线；播放头是三维空间中**随播放移动**、场景静止不动的标记。两者不混用。
- **声部轨（Staff Track）**：一条谱表在三维场景中对应的音符序列；各声部轨沿声部轴分层并列。
- **能量轨迹（Energy Trail）**：沿一条声部轨生成的连续发光带状几何，是三维场景的主体视觉。
- **光点（Glow Point）**：沿能量轨迹移动、标记当前播放时刻的发光体，为「播放头」的视觉载体。
- **配对 MIDI（Paired MIDI）**：为 MusicXML 乐谱提供速度映射、音频与力度的配套 MIDI 文件。乐谱本身只含图形与拍位，秒值、音长与力度均来自此文件。
- **谱面锚点（Score Anchor）**：音符在二维谱面中的像素坐标，用于二维谱面与三维场景之间的定位与转场。

## 测验（Quiz）词汇

- **测验轮次（Quiz Session）**：一次完整的答题过程，由若干道题组成，结束时产出成绩单。
- **题目（Question）**：目标和弦的一道单题 = 目标和弦 + 出题模式 + 4 个选项 + 展示音数据。
- **目标和弦（Target Chord）**：当前题目要求识别的和弦（根音 + 和弦类型）。
- **干扰项（Distractor）**：与正确答案相近但不同的错误候选（同根不同型 / 同型不同根 / 随机）。
- **选项（Option）**：一道题的候选答案，其中恰好一个是正确答案。
- **成绩单（Result Card）**：轮次结束时的统计汇总（得分、正确率、最佳连击、平均用时、逐题概况）。

## 出题模式（Mode）

- **视觉模式（Visual）**：以原位堆叠的五线谱呈现目标和弦。
- **音符乱序模式（Scrambled）**：目标和弦的同一组音以打乱的顺序、跨八度开放 voicing 呈现于谱面/钢琴。
- **听觉模式（Aural）**：仅播放和弦音响，不提供任何视觉提示。
- **混合模式（Mix）**：轮次内每道题从前述模式中随机抽取。

## 难度（Difficulty）

- **难度档案（Difficulty Preset）**：一组允许参与出题的和弦类型集合；三档预设（入门 / 进阶 / 专家），可扩展。

## 展示（Display）

- **展示方式（Display Mode）**：用户选择的视觉呈现偏好（键盘 / 五线谱 / 两者 / 自动）。
- **自动（Auto）**：除听觉模式恒无视觉提示外，其余模式等效于「两者」。
- **开放式 voicing（Open Voicing）**：将和弦音分布到多个八度、拉开间距的排列方式。

## 相关既有概念

- **调号（Key Signature）**：当前上下文使用的调（测验恒为 C 大调）。
- **音源（Sampler）**：播放音符的采样器引擎，测验与和弦显示共用。
