# CONTEXT.md — 领域术语表

本项目（MIDI-JAR）的领域概念。仅登记术语含义，不含实现细节。

## 核心领域

- **和弦显示（Chord Display）**：实时可视化用户弹奏的和弦（五线谱 + 钢琴 + 和弦名）。
- **和弦词典（Chord Dictionary）**：查询、分组浏览和弦的结构与排列。
- **和弦测验（Chord Quiz）**：系统出题、用户选择正确和弦名的测验。
- **乐谱滚动（Score Scroll）**：MusicXML 乐谱随播放滚动可视化的模块，时间轴由乐谱自身的音符时值与速度标记决定。
- **三维乐谱（3D Score）**：把整首曲子铺入三维空间、随播放推进视角的可视化模块。与乐谱滚动的区别：后者是二维视口内谱面相对固定扫描线移动，前者是静态场景内视角沿时间轴移动。

## 乐谱滚动（Score Scroll）词汇

- **扫描线（Scanline）**：视口内固定位置的竖直参考线，当前发声音符对齐于此，谱面相对其横向滚动。
- **谱面行位置（Staff Line Position）**：单行谱线在视口内的垂直位置（百分比）。
- **图元缓存（Primitive Cache）**：乐谱加载时从渲染结果解析出的一次性图形元素集合（矢量路径与文字记录），生成后只读，是绘制谱面的唯一数据来源。解析一次性异步完成（ADR 0013）：加载遮罩覆盖渲染与解析全程，完成后谱面完整可用，不存在"逐步浮现"或解析滞后空白。
- **画布渲染（Canvas Rendering）**：乐谱滚动的显示形态——谱面绘制在单一画布上，播放过程中不存在谱面文档节点。
- **可见窗口重绘（Visible Window Redraw）**：每帧仅绘制视口范围内的图元，视口外跳过；缩放无损由矢量图元保证。
- **飞入（Fly-in）**：音符在扫描线附近出现并飞入落位的动画；随机性由横向飞入距离、纵向散落与起步延迟三个可调分量构成。五线谱线（细长水平条：线宽 ≤4px 且长度 ≥60px）常驻直绘，完全跳过飞入编排（不 skip、不位移、不渐隐）；加线（ledger line）短于 60px 不豁免，跟随音符飞入。
- **飞入带（Fly-in Band）**：自扫描线向右延伸「飞入带宽度 + 起步延迟」的空间区间——扫描线 = 落位线，向右为飞入中（越远越未成形），带远端之外未显现（不绘制）；扫描线左侧恒为完成态。带起始随画布平移与播放推进同步变化：播放中音符临近发声时飞入，拖动画布使新内容滑入扫描线时同样飞入。带宽由「飞入带宽度」滑块调节（0-100，100 ≈ 300px，默认 150px）。进度条控制显现范围：拖到哪显示到哪，前进 = 推进、回退 = 收回。
- **飞出（Fly-out）**：与飞入镜像的消失动画——音符越过扫描线（已播放）后向左飞出并淡入消失。开关默认开启；飞出距离、纵向散落、起步延迟、飞出带宽度均独立可调。
- **飞出带（Fly-out Band）**：自扫描线向左延伸「飞出带宽度 + 起步延迟」的空间区间——扫描线 = 飞出起点，向左为飞出中（越远越透明），越过带远端即完全消失（不绘制）。与飞入带共用扫描线起点。
- **尾部静默（Tail Silence）**：时间轴在末音符结束后延长 2 秒（PLAYBACK_TAIL_SEC）的静默期——期间谱面 X 按末段斜率继续外推，扫描线得以完全越过乐谱末端；导出视频包含该尾部。
- **符头染色（Notehead Tint）**：播放头附近音符的颜色向染色色平滑插值，随与播放头的距离衰减，直接改变谱面墨迹颜色。染色对象仅限符头——通过音符外接矩形与图元中心点的几何匹配标记（ADR 0017），染色锚点是播放头位置，与画布平移无关。
- **自定义背景（Custom Background）**：谱面背景三态（主题色 / 纸张色 / 自定义颜色）之一；选择自定义时由取色器指定任意背景色，旧版「纯黑」背景自动迁移为自定义黑色。
- **帧参数构建（Frame Builder）**：把 0-100 的显示设置（飞入、高光、扫描线等）与主题解析色换算为一帧绘制所需参数的纯函数层；实时渲染与视频导出共用，保证两条路径画面一致。
- **视频导出（Video Export）**：把乐谱播放动画离线渲染为视频文件（当前为静音版）的功能。可选比例、分辨率、格式与帧率；采用 WebCodecs 编码 + Mediabunny 封装（ADR 0016），导出速度不受播放时长约束。

## 三维乐谱（3D Score）词汇

- **三维乐谱（3D Score）**：把整首曲子的音符一次性摆进三维空间、随播放推进视角的可视化模块。
- **时间轴（Time Axis）**：场景中展开时间维度的方向，单位为拍（beat），经速度段换算为秒。
- **音高轴（Pitch Axis）**：场景中展开 MIDI 音高维度的方向。
- **声部轴（Staff Axis）**：场景中区分谱表的维度；每个谱表在声部轴上占据独立的一层。
- **播放头（Playhead）**：场景中标记当前播放时刻的位置。与「扫描线」的区别：扫描线是二维视口内**固定**、谱面相对其移动的参考线；播放头是三维空间中**随播放移动**、场景静止不动的标记。两者不混用。
- **声部轨（Staff Track）**：一条谱表在三维场景中对应的符号集合；各声部轨沿声部轴分层并列，显隐粒度为整条谱表的符号块。
- **符号实体（Score Glyph Solid）**：从二维谱面渲染结果中提取的符号轮廓（SMuFL 字形 path、五线谱线）挤出成的三维浮雕体。符号保持谱面原始布局直铺于场景（谱面直铺），是三维场景的主体视觉；每个符号可独立寻址，供播放高亮使用。
- **能量轨迹（Energy Trail）**：穿过各符号锚点的细引导线，标示播放动线；自符号实体成为主体视觉后降级为辅助元素（ADR 0018）。
- **光点（Glow Point）**：（已退役）沿能量轨迹移动、标记当前播放时刻的发光体；自追迹小球引入后由主球承担播放指示与相机跟随目标（ADR 0019）。
- **追迹小球（Tracer Ball）**：每声部轨一组随播放逐音符弹跳的小球，球数与当前发声音符数一致——和弦变大时分裂出新球、变小时最远的球飞向下一音符后融入消失（ADR 0019）。第一声部的主球同时承担播放指示与相机跟随目标。
- **相机目标跟随（Camera Target Follow）**：三维乐谱的相机控制模式——用户通过轨道控制自由旋转、缩放、平移；播放时仅控制器的注视目标平滑跟随播放头，手动操作不中断跟随，「回到播放头」可将目标吸回。
- **配对 MIDI（Paired MIDI）**：为 MusicXML 乐谱提供速度映射、音频与力度的配套 MIDI 文件。乐谱本身只含图形与拍位，秒值、音长与力度均来自此文件。
- **谱面锚点（Score Anchor）**：音符在二维谱面中的像素坐标，用于二维谱面与三维场景之间的定位与转场。

## 测验（Quiz）词汇

- **测验轮次（Quiz Session）**：一次完整的答题过程，由若干道题组成，结束时产出成绩单。
- **题目（Question）**：目标和弦的一道单题 = 目标和弦 + 出题模式 + 4 个选项 + 展示音数据。
- **目标和弦（Target Chord）**：当前题目要求识别的和弦（根音 + 和弦类型）。
- **干扰项（Distractor）**：与正确答案相近但不同的错误候选（同根不同型 / 同型不同根 / 随机）。
- **选项（Option）**：一道题的候选答案，其中恰好一个是正确答案。
- **成绩单（Result Card）**：轮次结束时的统计汇总（得分、正确率、最佳连击、平均用时、逐题概况）。

## 出题模式（Mode）

- **视觉模式（Visual）**：以原位堆叠的五线谱呈现目标和弦。
- **音符乱序模式（Scrambled）**：目标和弦的同一组音以打乱的顺序、跨八度开放 voicing 呈现于谱面/钢琴。
- **听觉模式（Aural）**：仅播放和弦音响，不提供任何视觉提示。
- **混合模式（Mix）**：轮次内每道题从前述模式中随机抽取。

## 难度（Difficulty）

- **难度档案（Difficulty Preset）**：一组允许参与出题的和弦类型集合；三档预设（入门 / 进阶 / 专家），可扩展。

## 展示（Display）

- **展示方式（Display Mode）**：用户选择的视觉呈现偏好（键盘 / 五线谱 / 两者 / 自动）。
- **自动（Auto）**：除听觉模式恒无视觉提示外，其余模式等效于「两者」。
- **开放式 voicing（Open Voicing）**：将和弦音分布到多个八度、拉开间距的排列方式。

## 相关既有概念

- **调号（Key Signature）**：当前上下文使用的调（测验恒为 C 大调）。
- **音源（Sampler）**：播放音符的采样器引擎，测验与和弦显示共用。

## 通用控件（UI Controls）词汇

- **选项滑条（Option Slider）**：以滑轨加刻度的形式在离散选项中做单选的通用控件，用于替代下拉选择（select）与单选组（radio）。组件内部只认数值索引，领域值（如调号 "C"、"G"）由调用方在边界完成映射。
- **无填充（No Fill）**：选项滑条的默认形态——轨道上不绘制已选进度填充，只有滑头与刻度。
- **刻度标注（Tick Labels / Measure）**：显示在刻度下方的选项文字。选项过多或标签过宽时按宽度感知自动抽稀（首尾保留），当前选中值以徽标常显，信息不丢失。
- **双头范围滑条（Dual Range Slider）**：一根轨道上两个滑头框定数值范围 [lo, hi] 的通用控件。两滑头互不穿越；重合后继续拖动会推挤对方，被推挤的一方置顶以便拖开。
- **范围填充色（Range Fill Color）**：双头滑条两滑头之间（及单头滑头左侧）的填充颜色。既可用主题色关键字（随主题联动），也可指定任意颜色值（优先级更高）。
- **取色器（Color Picker）**：以触发按钮加浮层面板形式选取颜色的通用控件。组件值模型为纯 hex 颜色串（默认 6 位，启用透明度时 8 位），领域语义（如 daisyUI token 名）由调用方在边界处理；对无法解析的输入（如 "transparent"）只做兜底显示，不产生歧义值。
- **色环（Hue Ring）**：取色器面板外圈的圆环形色相选择带，指针角度即当前色相。
- **选色三角（Selection Triangle）**：色环内接的等边三角形，三顶点为当前色相的纯色与白、黑；三角内取点决定颜色的饱和度与明度。
- **互补标记（Complementary Marker）**：显示在选色三角内的虚线小圆，标记当前颜色的互补色（色相相对、饱和度/明度相同），点击即切换为互补色。与取色光标（实线圆）样式区分。
- **和谐色（Harmonies）**：以当前颜色为基准按色彩理论生成的配色方案色块（互补、邻近、三角、分裂互补、四角），用于「对比色」挑选。悬停仅预览，点击才应用。
- **最近使用（Recent Colors）**：取色器自动记录的历史选中颜色，全局持久化、有数量上限、去重置顶。
- **明暗阶（Tints & Shades）**：以当前颜色向白/黑方向按比例混合生成的阶梯色块行，点击即选。
- **吸管（Eyedropper）**：从屏幕任意位置拾取颜色的功能；运行环境不支持时入口自动隐藏。
