# 2026-07-09 工作日志

## 物理建模钢琴音源（Physical Piano Engine）— 完整实施

### 基于《电子合成器原理与创作方法》教程 v2 改进

基于教程第6/7/13章建议，对原有计划进行了 8 项改进并完整实施：

### 新增文件
- `public/audio-worklets/physical-piano-processor.js` — AudioWorkletProcessor 波导 DSP（含延迟线+分数延迟、循环滤波+T60、allpass频散、槌击激发(strikePosition)、音板共鸣8带通+Q差异化、交感共振、voice-stealing 4音保护区、参数平滑）
- `src/views/WaterfallPiano/audio/SoundEngine.ts` — SoundEngine 接口
- `src/views/WaterfallPiano/audio/OutputChain.ts` — 共享输出链（Tone.Gain 桥接 + filter→compressor→reverb→volume→destination）
- `src/views/WaterfallPiano/audio/PhysicalPianoEngine.ts` — SoundEngine 的 AudioWorklet 实现（Blob URL 加载）
- `src/views/WaterfallPiano/audio/physicalPianoProcessor.ts` — ?raw 导入加载器
- `src/views/WaterfallPiano/composables/useRealtimeMidi.ts` — 实时 MIDI 订阅（chord-display/default，带 velocity/CC64）

### 修改文件
- `types.ts` — AudioPreset + PhysicalPianoConfig + WaterfallPianoSettings 扩展
- `constants.ts` — 预设注册 + 默认 config + physicalPiano 字段
- `audio/AudioEngine.ts` — 改造实现 SoundEngine 接口，接收 OutputChain
- `engine/WaterfallEngine.ts` — 持有 OutputChain+SoundEngine，支持引擎切换
- `stores/waterfallPiano.ts` — 合并防护
- `WaterfallPiano.vue` — 接入 useRealtimeMidi
- `components/SettingsPanel.vue` — 预设选项 + 6 滑块 + polyphony
- `__tests__/WaterfallEngine.test.ts` — Mock 补全新方法
- `__tests__/constants.test.ts` — 更新预设计数为7

### 关键改进（v2）
1. 多维力度映射（amplitude + 谱展宽 + 衰减速率）
2. 踏板交感共振（voices间低增益耦合）
3. 音板模态 8 个带通 + Q 差异化 + 踏板增益耦合
4. 低音区 Thiran allpass 分数延迟（midi ≤ 48）
5. 显式 T60 公式 `g·|H|^(fs·T60/N) = 0.001`
6. 槌击位置 strikePosition(默认 1/8)
7. Voice-stealing 4音保护区
8. Worklet 提前验证

### 验证结果
- 测试: 162 passed / 5 skipped, 0 failures
- TypeScript: 无新增错误
