# 2026-07-06 工作日志

## HIG 人机界面优化全案报告

- **产出**：`outputs/HIG-optimization-report.html`（自包含 HTML，内联 CSS + SVG 标注图）
- **范围**：`src/views/` 下全部 15 个页面级组件逐行源码审计
- **框架**：基于 Apple HIG + Material Design 3，按 6 维度（视觉层级/间距节奏/触达与无障碍/动效反馈/深浅色适配/响应式与密度）评估
- **发现**：
  - 5 个 P0 阻断级缺陷（oklch 变量误用导致背景透明、Wire.vue hsl 色彩空间不一致、text-[10px] 违反最小可读字号）
  - 24 个 P1 高优先级（aria-current 缺失、btn-xs 点击目标过小、dialog aria-labelledby 缺失、透明度变体不统一、长表单缺锚点等）
  - 大量 Magic Number（text-3xl/5xl、gap-20、min-h-[150px]、duration 硬编码等）
- **交付**：Token 扩展规范（字号/间距/圆角/触达/动效/状态色 6 类 CSS 变量）+ Tailwind 映射方案 + 15 页 before/after SVG 标注图 + 6 维度评估表 + 分优先级优化清单 + 4 阶段实施路线图 + 成功指标
- **关键技术发现**：
  - DaisyUI v4 CSS 变量是纯数字（如 `--b2: 90%`），写 `oklch(var(--b2))` 缺 alpha 通道会解析失败，正确写法 `oklch(var(--b2) / 1)` 或用语义类 `bg-base-200`
  - WaterfallPianoSettings.vue 1268 行是最大设置页，急需页内锚点 TOC
  - 项目已有良好基础：i18n 全覆盖、prefers-reduced-motion 降级、语义化 role/aria 广泛使用

## HIG 优化执行 — P0 + P1 完成

### P0 阻断级修复（全部完成）
- **DaisyUI v4→v5 变量迁移**：AppNavbar、AppBreadcrumb 中 `--b2`/`--bc`/`--p` 等 v4 变量名在 v5 中不存在，改为 `--color-base-200`/`--color-base-content`/`--color-primary`
- **hsl 包裹 oklch 修复**：Wire.vue、MidiFlowGraph.vue、graphUtils.ts 中 `hsl(var(--color-error) / 0.85)` 无效（`--color-error` 是 oklch 值），改为 `color-mix(in oklch, var(--color-error) 85%, transparent)`
- **最小字号修复**：`text-[10px]` → `text-xs`（ChordOverview、SettingsLayout）

### Token 层创建
- 新建 `src/styles/hig-tokens.css`：12 级字号（11px–64px）、8 级间距（4px–64px）、4 级圆角、动效时长/缓动、语义状态色（success/warning/error/info + container 变体 + 深色模式反转）
- `tailwind.css` 中 `@import "./hig-tokens.css"` 加载 Token 层
- 使用 Tailwind v4 `@theme` 指令自动生成 `text-hig-*`/`rounded-hig-*`/`spacing-hig-*`/`ease-hig-*` 工具类

### P1 高优先级修复（全部完成）
- **aria-current + dialog ARIA**：SettingsLayout、ChordDisplayList 的 RouterLink 补充 `:aria-current="isActive ? 'page' : undefined"`，dialog 补充 `aria-labelledby`
- **btn-xs → btn-sm**：全局替换（保留 4 处紧凑上下文的 btn-xs：LatencyMonitor 徽章、ChordDictionaryToolbar 响应式、ChordSearch 输入框内嵌）
- **语义化状态色**：AppNavbar 延迟指示灯/录制脉冲灯从硬编码 oklch 改为 `var(--hig-success)`/`var(--hig-error)`/`var(--hig-info)` 等 Token 变量
- **统一透明度**：ChordDetail.vue 中 `/20`/`/30`/`/40` 散乱透明度统一为 `bg-base-200`/`bg-base-300`（实色）；ChordMenuGroup、ChordMenuItem、ChordSearchOption、SettingsLayout、SettingsRadioGroup 的 hover 透明度统一
- **Debugger ARIA live region**：日志容器补充 `role="log"` + `aria-live="polite"` + `aria-label`；toolbar 补充 `role="toolbar"`；切换按钮补充 `:aria-pressed`
- **长表单锚点 TOC**：新建 `SettingsToc.vue` 组件（IntersectionObserver scroll-spy + 点击展开折叠面板 + sticky 定位 + 响应式 lg 显示），集成到 ChordDisplayModuleSettings（7 节）和 WaterfallPianoSettings（12 节）；SettingsCollapse 新增 `id` prop 透传
- **i18n 新增**：`common.tableOfContents`（Contents/目录）、`settings.debuggerSettings.messageLog`（MIDI message log/MIDI 消息日志）

### 构建验证
- `vite build` 4.03s 通过，无 CSS/TS 错误
